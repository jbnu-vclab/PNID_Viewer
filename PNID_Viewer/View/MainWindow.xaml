<Window x:Class="PNID_Viewer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PNID_Viewer"
        xmlns:vm="clr-namespace:PNID_Viewer.ViewModel"
        xmlns:command="clr-namespace:PNID_Viewer.ViewModel.Commands"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.Resources>
        <command:OpenImageCommand x:Key="OpenImage"/>
        <command:OpenXmlCommand x:Key="OpenXml"/>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Menu Grid.Row="0" HorizontalAlignment="Left" VerticalAlignment="Top">
            <MenuItem Header="Open">
                <MenuItem Header="XML" Command="{Binding Source={StaticResource OpenImage}}"/>
                <MenuItem Header="Image" Command="{Binding Source={StaticResource OpenXml}}"/>
            </MenuItem>
        </Menu>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="3*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <!-- scrollviewer Auto, image stretch None-->
<!--
                     <ScrollViewer Grid.Column="0" HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Disabled">
                <Image Source="{Binding Source={StaticResource OpenImage}, Path=ImagePath, UpdateSourceTrigger=PropertyChanged}"
                       Stretch="Uniform"/>
            </ScrollViewer>   
-->
            <vm:ZoomBorder x:Name="border" ClipToBounds="True" Background="Gray">
                <Canvas x:Name="canvas">
                    <Canvas.Background>
                        <!--이미지를 imageBrush로 넣어도 될까?-->
                        <ImageBrush ImageSource="{Binding Source={StaticResource OpenImage}, Path=ImagePath, UpdateSourceTrigger=PropertyChanged}"/>
                    </Canvas.Background>
                </Canvas>
            </vm:ZoomBorder>
            <GridSplitter Grid.Column="1" Background="LightGray" Width="5" HorizontalAlignment="Center" VerticalAlignment="Stretch" ShowsPreview="True"/>
            <Grid Grid.Column="2">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <StackPanel Grid.Row="0">
                    <TextBox Text="{Binding ElementName=canvas, Path=ActualHeight, UpdateSourceTrigger=PropertyChanged, Mode=OneWay}"/>
                    <TextBox Text="{Binding ElementName=canvas, Path=ActualWidth, UpdateSourceTrigger=PropertyChanged, Mode=OneWay}"/>
                </StackPanel>
                    <GridSplitter Grid.Row="1" Background="LightGray" Height="5" HorizontalAlignment="Stretch" VerticalAlignment="Center" ShowsPreview="True"/>
                <StackPanel Grid.Row="2"/>
            </Grid>
        </Grid>
    </Grid>
</Window>
